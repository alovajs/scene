// import { T$, Tupd$, Twatch, T_$, T_exp$ } from '@/framework/type';
// import { createAssert } from '@/helper';
// import { AlovaMethodHandler, Method } from 'alova';
// import { RetriableHookConfig } from '~/typings/general';

// const assert = createAssert('useRetriableRequest');
// export default <S, E, R, T, RC, RE, RH>(
//   handler: Method<S, E, R, T, RC, RE, RH> | AlovaMethodHandler<S, E, R, T, RC, RE, RH>,
//   config: RetriableHookConfig<S, E, R, T, RC, RE, RH, F>,
//   $: T$,
//   _$: T_$,
//   _exp$: T_exp$,
//   upd$: Tupd$,
//   watch: Twatch
// ) => {};
